# 项目 Bug 审查报告

审查时间：2026-07-04  
范围：后端启动、文档上传与 worker 入库、RAG 检索/生成、会话流式接口、权限过滤、迁移、前端 API 契约。  
约束：未阅读本目录下已有 Markdown 文档；本文件仅为新增报告，未修改业务代码。

## 验证结果

- `python -m compileall apps\backend\app apps\backend\tests`：通过
- `python -m compileall apps\backend\alembic scripts`：通过
- `python scripts\verify_migrations.py`：通过
- 在 `apps/backend` 下运行 `python -m unittest discover -s tests`：63 个测试通过
- `npm run build`：前端构建通过
- `python scripts\check_project.py --skip-compose`：通过

没有运行 `scripts/smoke_demo.py`：它需要完整运行中的外部服务栈，并会读取默认 demo Markdown 文件；为遵守“不阅读 Markdown”约束，本次没有触发。

## 运行 Pipeline 摘要

实际链路大致是：

1. FastAPI 启动：`init_db()`，尝试 `ensure_qdrant_collection()`。
2. 上传文档：`create_uploaded_document()` 写 DB、上传 MinIO、提交 DB、投递 Celery 任务。
3. Worker 入库：`process_document()` 从 MinIO 下载、解析、切块、写 DB chunks、写 Qdrant points。
4. 问答：`build_rag_answer()` 同时走 dense Qdrant 和 BM25 DB 检索，RRF 融合，压缩上下文，调用 LLM。
5. 会话：`stream_message_response()` 先落用户消息，再用后台线程跑 agent graph，边生成边 SSE 推送。

## 发现的问题

### 1. 删除文档后，Qdrant 清理失败会让已删除内容继续被检索

严重级别：高

证据：

- `apps/backend/app/services/document_service.py:179-180` 先 `db.delete(document)` 并 `db.commit()`。
- `apps/backend/app/services/document_service.py:193-197` 之后才 `remove_object()` 和 `delete_document_vectors()`，而 Qdrant 删除异常被吞掉。
- `apps/backend/app/rag/retrieval.py:37-53` dense retrieval 直接信任 Qdrant payload 里的 `content`、`file_name`、`document_id`，没有回查 DB chunk/document 是否仍存在。

影响：

如果 Qdrant 在删除时短暂不可用，DB 里的文档已经消失，但 Qdrant point 仍保留完整正文 payload。之后同一个知识库的 dense 检索仍可能返回已删除文档内容，导致数据删除不生效。

建议方向：

删除向量失败时不要静默成功；至少记录失败状态并阻断后续检索。更稳妥的是在检索返回后回查 DB chunk/document 状态，或使用 outbox/重试任务确保 Qdrant 清理最终完成。

### 2. 删除知识库不会清理 MinIO 对象，且 Qdrant 清理失败也被吞掉

严重级别：高

证据：

- `apps/backend/app/services/knowledge_base_service.py:104-111` 删除知识库只删除 DB 记录并尝试 `delete_knowledge_base_vectors(kb_id)`。
- 该路径没有遍历知识库下的 `Document.object_key`，也没有调用 `remove_object()`。
- Qdrant 删除异常同样被 `except Exception: pass` 吞掉。

影响：

知识库删除后，MinIO 中的原始上传文件会残留，形成存储泄漏和数据保留风险。Qdrant 清理失败时也没有任何可见失败信号。

建议方向：

删除知识库前收集文档 object keys；DB 删除、对象删除、向量删除需要有可追踪的补偿或重试机制，不能静默吞掉。

### 3. 上传文档先提交 DB 再投递 Celery，入队失败会留下卡在 `uploaded` 的文档

严重级别：中高

证据：

- `apps/backend/app/services/document_service.py:102` 上传文件到 MinIO。
- `apps/backend/app/services/document_service.py:105` 提交文档记录。
- `apps/backend/app/services/document_service.py:106` 才调用 `process_document.delay(document.id)`。

影响：

如果 Redis/Celery broker 在这个点失败，接口会抛错，但 DB 文档和 MinIO 对象已经提交，文档会停在 `uploaded`，没有 job 会处理它。用户侧看到的是上传失败或后续一直不入库。

建议方向：

捕获入队失败并把文档标记为 `failed`，或引入可重试的任务 outbox。返回给前端前应保证“已提交的文档一定有可恢复的处理任务”。

### 4. `/ready` 不检查 Celery worker，smoke pipeline 可能 ready 但永远不索引

严重级别：中

证据：

- `apps/backend/app/core/health.py:17-22` readiness 只检查 database、redis、qdrant、minio。
- `infra/docker-compose.yml:93-104` worker 是单独服务，但没有 healthcheck，也没有被 `/ready` 覆盖。
- `scripts/smoke_demo.py:33` 先等 `/ready`，随后上传文档并在 `scripts/smoke_demo.py:182` 轮询 indexed。

影响：

Redis 可用但 worker 未启动/已挂时，后端仍会 ready，上传接口仍能返回 job id，但文档不会从 `uploaded` 进入 `indexed`，smoke test 会超时。

建议方向：

readiness 增加 worker 活性检查，或单独暴露 ingestion readiness。Compose 里的 worker 也应有 healthcheck。

### 5. Worker 重建索引时先删旧向量，再写新向量，失败会造成 DB/Qdrant 不一致

严重级别：中

证据：

- `apps/backend/app/workers/document_tasks.py:53-54` 删除 DB chunks 并调用 `delete_document_vectors(document.id)`。
- `apps/backend/app/workers/document_tasks.py:73-78` 之后才 upsert 新向量并提交 DB。
- `apps/backend/app/workers/document_tasks.py:81-93` 失败后 rollback，然后把文档设为 failed，并再次尝试删向量；删除失败仍被吞掉。

影响：

如果这是一次重建索引，旧向量先被删除。后续 embedding/Qdrant upsert/DB commit 任一步失败，都可能让旧 DB chunks、文档状态和 Qdrant points 不一致。极端情况下会出现 DB 有 chunks 但 dense 检索无向量，或 Qdrant 有孤儿 points。

建议方向：

使用新 point 集合或临时版本写入成功后再切换；旧向量删除失败不能静默；检索层最好以 DB 中 indexed 状态作为最终约束。

### 6. BM25 候选预取无排序截断，大知识库会漏掉高分块

严重级别：中

证据：

- `apps/backend/app/rag/advanced_retrieval.py:220-243` 先用 `ILIKE` 找匹配 chunk。
- `apps/backend/app/rag/advanced_retrieval.py:243` 直接 `.limit(max(limit * BM25_PREFILTER_MULTIPLIER, limit))`，没有 `ORDER BY`。
- BM25 分数只在这批被 DB 随机/物理顺序返回的候选上计算。

影响：

当匹配 chunk 数量超过预取上限时，真正 BM25 分最高的 chunk 可能没有进入内存排序，导致召回不稳定。知识库越大越明显。

建议方向：

用数据库全文检索或至少提高/配置化预取，并加入可解释排序条件。更根本的是让 DB 侧产生候选分数，而不是无序截断后再算分。

### 7. chunk 合并时只保留第一个 block 的 metadata，引用页码/章节可能错误

严重级别：中低

证据：

- `apps/backend/app/rag/splitters.py:25` 用单个 `buffer_meta` 保存合并 chunk 的元数据。
- `apps/backend/app/rag/splitters.py:52` 后续 block 进入同一 chunk 时不会更新 metadata。
- `apps/backend/app/rag/splitters.py:31-32` flush 时用第一个 block 的 page/section/title 生成整个 chunk。

影响：

如果一个 chunk 合并了多个 PDF 页、DOCX 段落或 CSV 批次，引用的 `page_number`、`section_name`、`title_path` 可能只代表第一段，不代表 chunk 中实际命中的文本。

建议方向：

在 page/section/title 变化时强制 flush，或在 chunk metadata 中保存页码/章节范围。

### 8. 前端只在首次加载时刷新 token，长会话中 API/SSE 会在 60 分钟后直接失败

严重级别：中低

证据：

- `apps/backend/app/core/config.py:50` access token 默认 60 分钟过期。
- `apps/frontend/src/App.tsx:27-48` 只在应用初始化 `fetchMe` 失败时 refresh。
- `apps/frontend/src/lib/api.ts:659-675` 普通 `apiRequest()` 遇到 401 只抛错，没有 refresh/retry。
- `apps/frontend/src/lib/api.ts:597-621` SSE 请求也没有 refresh/retry。

影响：

用户打开页面超过 60 分钟后继续上传、聊天或查看日志，会得到认证错误，必须刷新页面才能恢复。

建议方向：

把 refresh/retry 放到统一 API 客户端中；SSE 建连前也应检查或刷新 access token。

### 9. “停止生成”只 abort 前端 fetch，后端 agent 线程没有取消机制

严重级别：中低

证据：

- `apps/frontend/src/pages/ChatPage.tsx:392-393` stop 只是 `AbortController.abort()`。
- `apps/backend/app/services/conversation_service.py:211-272` 后端用独立 `Thread` 运行 `run_agent()`，没有接收取消信号。

影响：

用户点击停止后，前端本地标记停止，但后端 LLM 调用可能继续运行、继续产生成本，并可能最终落库 assistant message。刷新页面后可能看到一个“已停止”操作之后仍生成完成的回答。

建议方向：

为 stream request 建立可传递到 worker/LLM 层的取消状态；前端 stop 后后端应停止生成或至少避免落库为正常完成。

### 10. 迁移和模型存在漂移，迁移校验没有发现

严重级别：低

证据：

- `apps/backend/alembic/versions/20260703_0001_initial_schema.py:185` 创建了 `retrieval_logs.reranker_enabled`。
- `apps/backend/app/db/models/retrieval_log.py` 没有对应字段，API schema 也没有。
- `apps/backend/app/db/models/refresh_token.py:17` 模型声明 `token_hash` 为 unique。
- `apps/backend/alembic/versions/20260703_0001_initial_schema.py:55` 迁移只创建普通 index，没有 `unique=True`。
- `scripts/verify_migrations.py:48-61` 只检查缺表/缺列，不检查多余列、唯一约束、索引、nullable、类型差异。

影响：

用 `Base.metadata.create_all()` 初始化的开发库和用 Alembic 初始化的生产库并不完全一致。当前行为未必马上出错，但后续 autogenerate、约束依赖或排查问题时会混淆。

建议方向：

修正迁移与模型的一致性；增强迁移校验，至少检查 extra columns 和关键 unique/index 约束。

## 额外备注

- 从仓库根目录直接运行 `python -m unittest discover -s apps/backend/tests` 会导入环境中的第三方 `tests.helpers`，导致失败。项目自带 `scripts/check_project.py` 通过设置 `PYTHONPATH` 避开了这个问题，所以这不算核心 bug，但容易误导手动测试。
- 本次没有发现会让当前单元测试或前端构建失败的语法问题；主要问题集中在真实外部服务 pipeline 的一致性、可恢复性和长会话运行时行为。
